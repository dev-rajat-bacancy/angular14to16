import { ClassStandaloneDirective } from './class-standalone.directive';

describe('ClassStandaloneDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassStandaloneDirective();
    expect(directive).toBeTruthy();
  });
});
